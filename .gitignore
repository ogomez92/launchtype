# build
/target
/dist

# runtime user data (portable files living next to the exe during dev runs)
/*.json
/snippets/
/screenshots/
# a dev-run vault: encrypted, but a password store has no business in a repo
/vault/

# fixtures are real data and ARE committed
!/fixtures/**
