fn main() {
    // wxDragon app shell lands in M6; this placeholder keeps the workspace
    // building (and links prism so the FFI is exercised by `cargo build`).
    println!("launchtype-rs: work in progress");
}
